@{{agent_file}}

# GEMINI.md

Gemini CLI loads `GEMINI.md` by default. Antigravity can consume `{{agent_file}}`
directly.

Shared repo guidance lives in `{{agent_file}}`; this file exists so
Gemini-family tooling gets the same harness entrypoint without duplicating it.
